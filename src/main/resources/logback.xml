<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

	<!--输出到控制台的appender -->
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<!-- encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder 
			by default -->
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36}
				- %msg%n
			</pattern>
		</encoder>
	</appender>


	<!-- 记录到文件的appender -->
	<appender name="FILE" class="ch.qos.logback.core.FileAppender">
		<file>myApp.log</file>
		<append>false</append>
		<!-- encoders are assigned by default the type ch.qos.logback.classic.encoder.PatternLayoutEncoder -->
		<encoder>
			<pattern>%date %level [%thread] %logger{10}[%file:%line] %msg%n
			</pattern>
		</encoder>
	</appender>


	<appender name="DB-C3P0" class="ch.qos.logback.classic.db.DBAppender">
		<connectionSource class="ch.qos.logback.core.db.DataSourceConnectionSource">

			<!--使用c3p0连接池 -->
			<dataSource class="com.mchange.v2.c3p0.ComboPooledDataSource">

				<driverClass>com.mysql.jdbc.Driver</driverClass>
				<jdbcUrl>jdbc:mysql://localhost:3306/logback</jdbcUrl>
				<user>root</user>
				<password>root</password>
			</dataSource>
		</connectionSource>
	</appender>

	<appender name="DB-DRUID" class="ch.qos.logback.classic.db.DBAppender">
		<connectionSource
			class="ch.qos.logback.core.db.DriverManagerConnectionSource">
			<!--druid连接池  -->
			<dataSource class="com.alibaba.druid.pool.DruidDataSource">
				<!-- 使用dbcp连接池 -->
				<!-- <dataSource class="org.apache.commons.dbcp.BasicDataSource"> -->
				<driverClass>com.mysql.jdbc.Driver</driverClass>
				<url>jdbc:mysql://localhost:3307/logback_demo</url>
				<user>root</user>
				<password>root</password>
			</dataSource>
		</connectionSource>
	</appender>



	<!-- 根目录的输出级别 -->
	<root level="debug">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="FILE" />
		<appender-ref ref="DB-DRUID" />
	</root>

	<!-- 单独类的输出设定 -->
	<!-- <logger name="com.example.demo"> <appender-ref ref="DB-DRUID" /> </logger> -->



</configuration>